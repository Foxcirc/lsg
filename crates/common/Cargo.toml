[package]
name = "common"

version.workspace = true
authors.workspace = true
edition.workspace = true

[dev-dependencies]
futures-lite = { version = "2.6.1", default-features = false, features = ["alloc", "std"] }
